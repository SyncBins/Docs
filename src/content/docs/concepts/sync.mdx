---
title: How sync works
description: WebSocket fanout, version counters, the catch-up flow after a reconnect. Why we don't use CRDTs (last-write-wins is correct for a single user).
sidebar:
  order: 2
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';

WebSocket fanout, version counters, the catch-up flow after a reconnect. Why we don't use CRDTs (last-write-wins is correct for a single user).

<Aside type="note" title="Coming soon">
This page is part of the redesigned IA but the content hasn't landed yet. The KNOWLEDGE-BASE doc has a draft outline; drop a comment on [the docs issue](https://github.com/SyncBins/Docs/issues) if you want to help write it.
</Aside>
