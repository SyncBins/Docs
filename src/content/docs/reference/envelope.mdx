---
title: Encryption envelope
description: Exact byte layout of the on-wire `ct` field — nonce, AAD, ciphertext, auth tag. Wrapped-key bundle shape per device pubkey. Useful when implementing a non-JS client.
sidebar:
  order: 3
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';

Exact byte layout of the on-wire `ct` field — nonce, AAD, ciphertext, auth tag. Wrapped-key bundle shape per device pubkey. Useful when implementing a non-JS client.

<Aside type="note" title="Coming soon">
This page is part of the redesigned IA but the content hasn't landed yet. The KNOWLEDGE-BASE doc has a draft outline; drop a comment on [the docs issue](https://github.com/SyncBins/Docs/issues) if you want to help write it.
</Aside>
