---
title: WebSocket protocol
description: Connection setup, message shapes (`hello`, `item`, `delete`, `caught_up`, `ping`/`pong`), heartbeat (30s ping / 90s grace), reconnect & resume semantics. Source of truth: `shared/src/ws.ts`.
sidebar:
  order: 2
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';

Connection setup, message shapes (`hello`, `item`, `delete`, `caught_up`, `ping`/`pong`), heartbeat (30s ping / 90s grace), reconnect & resume semantics. Source of truth: `shared/src/ws.ts`.

<Aside type="note" title="Coming soon">
This page is part of the redesigned IA but the content hasn't landed yet. The KNOWLEDGE-BASE doc has a draft outline; drop a comment on [the docs issue](https://github.com/SyncBins/Docs/issues) if you want to help write it.
</Aside>
