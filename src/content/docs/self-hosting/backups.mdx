---
title: Backups & restore
description: Snapshot the SQLite metadata; the blob backend handles its own durability (or your local disk does). Verify a restore in a scratch container before you need it.
sidebar:
  order: 4
---

import { Aside, LinkCard, CardGrid } from '@astrojs/starlight/components';

Snapshot the SQLite metadata; the blob backend handles its own durability (or your local disk does). Verify a restore in a scratch container before you need it.

<Aside type="note" title="Coming soon">
This page is part of the redesigned IA but the content hasn't landed yet. The KNOWLEDGE-BASE doc has a draft outline; drop a comment on [the docs issue](https://github.com/SyncBins/Docs/issues) if you want to help write it.
</Aside>
